<header class="blog-header py-3">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <div class="col-4 pt-1">
        
        <!--
        Subscriptor Options:
        1 - User es Subscriptor => Si lo puedo validar
        2 - User NO ES Subscriptor => No lo puedo validar
        3 - Subscriptor NO ES User => No lo puedo validar
        4 - Simple Visitor => No lo puedo validar
        5 - User, pero no está logueado => No lo puedo validar
        6 - Nuevo Subscriptor ya es User => Si se puede validar de otra forma (desde el controlador)
        -->

        <% unless user_signed_in? && current_user.subscriptor? %>
          <%= link_to "Subscribe", new_subscriptor_path, class: "text-muted"%>
        <% end %>
      </div>
      <div class="col-4 text-center">
        <%#<a class="blog-header-logo text-dark"  href="#">Amazon Blog</a>%>
        <%=link_to "Amazon Blog", posts_path, class: "blog-header-logo text-dark" %>
      </div>
      <div class="col-4 d-flex justify-content-end align-items-center">
        <a class="text-muted" href="#">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mx-3"><circle cx="10.5" cy="10.5" r="7.5"></circle><line x1="21" y1="21" x2="15.8" y2="15.8"></line></svg>
        </a>
        <%= render 'layouts/sign_in' %>
      </div>
    </div>
  </header>